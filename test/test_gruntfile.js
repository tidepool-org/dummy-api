'use strict';
var should = require('should');
describe('Tidepool dummy API', function(){
  it('should have dummy test', function(done) {
    var isTrue = true;
    should.exist(isTrue);
    done();
  });
});